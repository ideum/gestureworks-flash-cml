package com.gestureworks.cml.managers
{
	/*import flash.display.StageScaleMode;
	import flash.display.StageAlign;
	import flash.display.StageDisplayState;
	import flash.events.Event;
	import id.core.Application;
	import id.core.ApplicationGlobals;
	import id.module.AvatarViewer;
	import id.core.TouchSprite;
	import gl.events.GestureEvent;
	import flash.display.DisplayObject;
	
	import id.menu.caraselleMenu;
	import id.menu.conveyorMenu;
	import id.menu.coverflowMenu;*/


	public class MenuManager /*extends TouchSprite*/
	{
			/*private var objects:Array = new Array();
			private var n:int;
			private var centerX: Number;
			private var centerY:Number;
			
			private var opt:String = "conveyor"
			//private var opt:String = "coverflow";
			//private var opt:String = "caraselle";
			
			private var menu
			private var menuWidth;
			private var menuHeight;*/
		
		public function MenuManager()
		{
			/*menuWidth = 400;
			menuHeight = 400;*/
		}

		/*override protected function initialize():void
		{
			if(opt =="caraselle"){
				menu = new caraselleMenu();
			}
			if(opt == "coverflow"){
				menu = new coverflowMenu();
			}
			if(opt == "conveyor"){
				menu = new conveyorMenu();
			}
			
			menu.addEventListener(caraselleMenu.COMPLETE, menuLayout);
			addChild(menu);
		}
		
		private function menuLayout(event:Event):void
		{
			trace("menu layout complete")
			menu.x = -1000/2;//menu.width/2
			menu.y = -300/2;//menu.height/2;
			updateMenu();
		}
		
		public function updateMenu(){
			//trace("updating menu in manager")
			menu.updateMenu();
		}*/
		//------------------------------------------------------------//
	}
}